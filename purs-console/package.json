{
  "private": true,
  "scripts": {
    "build": "npx -c 'spago build --path ./gen/**/*.purs'",
    "test": "npx -c 'spago test --path ./gen/**/*.purs'",
    "dist": "cp -r static/* dist/ && npx -c 'spago bundle-app --path ./gen/**/*.purs --main Main --to ./dist/app.js'",
    "clean": "rimraf output && rimraf .pulp-cache && rimraf gen/ && rimraf dist/",
    "watch": "npx -c 'spago build --watch --path ./gen/**/*.purs'"
  },
  "devDependencies": {
    "parcel": "1.12.3",
    "pulp": "^12.0.0",
    "purescript": "^0.12.5",
    "purescript-psa": "^0.5.0",
    "purescript-suggest": "^2.2.0",
    "spago": "^0.8.3",
    "webpack": "^4.16.2",
    "webpack-cli": "^3.1.0"
  },
  "dependencies": {}
}
